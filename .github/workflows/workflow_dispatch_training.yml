name: Worflow dispatch

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      subscriptions:
        description: "Subscriptions"
        required: true
        type: string
        default: "value 1"

jobs:

  build:
    name: Subscriptions
    runs-on: ubuntu-latest

    steps:
    - name: Checkout latest commit
      uses: actions/checkout@v3

    - name: Show input values
      run: |
        echo "Subscriptions: {{ github.event.inputs.subscriptions }}"
